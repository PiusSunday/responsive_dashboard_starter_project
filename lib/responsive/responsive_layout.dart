import 'package:flutter/material.dart';

class RedponsiveLayout extends StatefulWidget {
  const RedponsiveLayout({
    super.key,
    // ToDo 2: require the scaffolds
  });

  // ToDo 1: Declare the scaffolds of a Widget type

  @override
  State<RedponsiveLayout> createState() => _RedponsiveLayoutState();
}

class _RedponsiveLayoutState extends State<RedponsiveLayout> {
  @override
  Widget build(BuildContext context) {
    
    // ToDo 3: Return the LayoutBuilder and return the different scaffold's based on the constraints
    return Container(); 
  }
}
